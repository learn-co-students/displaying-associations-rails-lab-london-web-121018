
<h1>Artists Page</h1>
<hr>
<% arr = [] %>
<% @artists.each do |artist|  %>
<h2> <%=link_to artist.name, artist_path(artist) %> </h2>
     <% arr = [] %>
         <% artist.songs.each do |song| %>
         <% arr << song %>
         <% end %>
         <h3> <%= arr.count %> songs </h3>
                 <% arr.each do |song| %>
            <li> <%= link_to song.title, song_path(song) %> </li>
                 <% end %>
<hr>
<% end %>
